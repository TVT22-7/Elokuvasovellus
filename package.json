{
  "scripts": {
    "test": "mocha",
    "build": "cd client && npm install && npm run build",
    "start": "node server/index.js"
  },
  "engines": {
    "node": ">=16.14"
  },
  "dependencies": {
    "react-scripts": "^3.0.1"
  }
}
