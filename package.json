{
  "scripts": {
    "test": "mocha",
    "build": "cd client && npm install && npm run build",
    "start": "node server/index.js && pnpm install dotenv"
  },
  "engines": {
    "node": ">=16.14"
  },
  "dependencies": {
    "dotenv": "^16.3.1",
    "react-scripts": "^3.0.1"
  }
}
